#!/usr/bin/env bash
# Keep the per-tool agent config mirrors in step with .agents/.
# Installed via: git config core.hooksPath .agents/githooks
exec "$(git rev-parse --show-toplevel)/.agents/sync.sh"
