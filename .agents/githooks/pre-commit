#!/usr/bin/env bash
# Lint skill frontmatter, then keep the per-tool agent config mirrors in step with .agents/.
# Installed via: git config core.hooksPath .agents/githooks
root="$(git rev-parse --show-toplevel)"
"$root/.agents/scripts/lint-skills.sh" || exit 1
exec "$root/.agents/sync.sh"
